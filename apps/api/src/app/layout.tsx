export const metadata = {
  title: 'Atlas API',
  description: 'Atlas Readiness Guide API',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
